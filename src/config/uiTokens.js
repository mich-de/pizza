/* Frammenti di classe condivisi fra le pagine, per non riscriverli.
   `CHIP_ACTIVE` e' il chip selezionato: fondo inchiostro, come la paletta
   girata del tabellone. Non usa l'ambra — quella segnala, non qualifica. */
export const CHIP_ACTIVE = 'bg-ink text-on-ink border-ink';
